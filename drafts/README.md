# Drafts

25 blog posts that are written but not published. Still StatiCrypt-encrypted
with the site password. `_config.yml` excludes this folder from GitHub Pages,
so nothing here is served.

To publish one: move it to `blog/<slug>/`, add its card to `blog/index.html`,
and fix the nav paths (drafts use `../` depth, published posts use `../../`).
