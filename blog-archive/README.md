# Blog archive

A complete, StatiCrypt-encrypted copy of ALL blog posts (both the 12 published
under /blog/ and the 25 unpublished under /drafts/). This folder is excluded
from GitHub Pages in _config.yml, so nothing here is reachable from the website
— it exists purely as an offline backup inside the repo.
